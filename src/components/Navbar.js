export default function Navbar(){
    return(
        <nav className="Nav--bar">
            <img src="./images/geo-Logo.avif" alt="football"/>
            <p>Safari Adventures Team</p>
        </nav>
    )
}